name: 1workflow
on: [push]

jobs:
  run-first-command:
    runs-on: ubuntu-latest
    steps:
      - name: display some text
        run: echo "some text"
      - name: command in multiline
        run: |
          node -v
          npm -v